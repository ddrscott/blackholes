export const config = {
    x_increment: 'auto', // pixels or auto
    y_increment: 'auto', // pixels or auto
    name: 'No name',
    minPoints: 300000,   // minPoints to pass this stage
    layout: `
First New line is always ignored
for easier formatting.`,
}
